# Dependencies
node_modules
.pnpm-store

# Build outputs
.next
dist
build
out

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.idea
.vscode
*.swp
*.swo

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Turbo
.turbo

# Misc
.DS_Store
*.pem
Thumbs.db

# TypeScript
*.tsbuildinfo

# Vercel
.vercel

# Kid's personal "player card" — LOCAL ONLY, never committed, never shared.
# Claude reads this to remember the kid by name and personalize games for them.
.claude/player-profile.json

# Auto-backups taken before editing a game, so "oops, go back" can restore it — local.
.game-backups/

# Claude Code local settings (machine-local — never commit)
.claude/settings.local.json
settings.local.json
