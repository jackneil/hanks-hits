VERDICT: NEEDS ATTENTION

- MEDIUM: The advisory helper is outside the allowed file inventory and is not exported by `@hank-neil/db`. The plan requires it at [line 780](...), but the inventory restricts files at [line 1025](...) and the package exports only `.` and `./schema` at [package.json:6](/Users/jack.neil/Github/hanks-hits/packages/db/package.json:6). Add the helper to scope and require a root re-export or explicit subpath export.

- MEDIUM: The typed API contract is incomplete. `logicalSaveId` is required in the response at [line 720](...), but the request body explicitly contains only `{data, merge:true}` at [line 458](...) and current transport sends only those fields at [useAuthSync.ts:143](/Users/jack.neil/Github/hanks-hits/apps/web/src/shared/hooks/useAuthSync.ts:143). Invalid app IDs cannot satisfy `appId: ValidAppId` at [line 723](...) despite being required 400s at [line 761](...). DELETE has no typed success envelope or `NOT_FOUND` code, and `MergeConflict` has no defined shape while the source uses `string[]` at [progress-merge.ts:14](/Users/jack.neil/Github/hanks-hits/apps/web/src/lib/progress-merge.ts:14). Define all request/response schemas and mappings.

- MEDIUM: The required-ID reporter is not attached to the executable harness. The command only invokes Vitest at [line 1158](...), while reporter wiring is stated only in prose at [line 1172](...). The expected IDs also contain one six-case ID at [line 1173](...) although the plan requires six separate tests at [line 793](...). Add explicit reporter wiring and six independently enforced IDs, or one aggregate test that asserts all six cases.

- MEDIUM: Concurrency tests cannot prove lock-grant order or fully correlate waiter, backend, and request. The plan requires request IDs and backend PIDs at [line 781](...), but defines no request-ID generation or pre-response observation seam. The POST/DELETE tests only observe waiters, release, then assert order at [line 1193](...). Add a correlated trace/barrier keyed by request ID and backend PID that records lock acquisition, read, DML, and commit order.