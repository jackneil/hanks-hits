You are GPT-5.6 Luna working as the sole reasoning and writing model for this task. Work in /Users/jack.neil/Github/hanks-hits.

Task: amend the existing implementation plan at docs/superpowers/plans/2026-08-03-progress-sync-integrity.html so it is genuinely implementation-ready. This is still a planning-only pass. Edit ONLY that HTML plan. Do not edit product source, tests, config, package files, lessons, or graphify artifacts. Preserve the polished HTML structure and CSS. Do not weaken any safety or proof requirement. Do not cap successful work.

Required inputs to read completely before editing:
- docs/superpowers/plans/2026-08-03-progress-sync-integrity.html
- graphify-out/.luna_final_client_review.txt
- graphify-out/.luna_final_server_review.txt
- graphify-out/.luna_final_policy_review.txt
- graphify-out/.luna_fresh_client_review.txt
- graphify-out/.luna_fresh_server_review.txt
- graphify-out/.luna_fresh_policy_review.txt
- graphify-out/.luna_dc_plan_client.txt
- graphify-out/.luna_dc_plan_server.txt
- graphify-out/.luna_dc_plan_premortem.txt
- lessons.md and all applicable repository instructions

Inspect current repository source whenever needed. Resolve EVERY CRITICAL and MEDIUM finding in the three final reports, including overlaps, and retain all already-corrected obligations from prior reports. Reconcile the document as one coherent executable contract rather than appending patches. In particular, the amendment must make these decisions explicit and testable:

1. Separate persisted projections from schema-valid request snapshots. Use a discriminated absent/valid/malformed persisted read with foundStorageKey, exact hydration state, raw persisted value, declared excluded persisted paths, semantic identity, meaningful-local classification, and a stable snapshot factory. Monster Truck and Oregon Trail synthetic timestamps must not churn semantic identity, markers, or beacon deduplication. Repeated reads without a mutation must be identical, while a logical save captures one stable schema-valid request snapshot.
2. Complete the first-sync truth table, including persisted default plus an existing server row. Define a clean semantic baseline that is not an acknowledgment and suppresses polling saves, force saves, and beacons while state remains non-meaningful. Server adoption must win in that missing row of the table. The first meaningful post-READY mutation must queue exactly once.
3. Define mounted-hook owner rotation and StrictMode behavior. Invalidation and owner change must synchronously reset acknowledged markers, semantic baselines, request sequences, force latches, queues, timers, abort controllers, and successful-beacon records; then a still-mounted hook must register a fresh owner/epoch handle and restart readiness. Cover same-mounted A-to-B, auth loss, same-tab sign-out, purge, broadcast, siblings, active GET/POST, and StrictMode cleanup/re-registration.
4. Deduplicate terminal beacons by owner, epoch, and canonical body, not by epoch alone. Record only a successful sendBeacon return. Define late foreground responses versus newer beaconed bodies. Cover hidden S0, resume/mutate S1, pagehide S1, same-body repeats, different bodies, acknowledged bodies, and sendBeacon=false.
5. Add a complete client failure/liveness matrix for initial GET, bootstrap POST, required re-fetch, normal POST, and aborts, including malformed JSON and HTTP 400/401/409/429/503/500. Classify retryable versus terminal states, Retry-After handling, bounded backoff/exhaustion diagnostics, recovery triggers, and retained queue semantics. Add forcePending through hydration/GET/bootstrap/re-fetch/active POST/backoff, and require a stable forceSync callback backed by a persistent coordinator reference.
6. Add the exact achievements caller path, apps/web/src/shared/components/AchievementCelebrations.tsx, and define ProgressSyncFixtureBuilder and its result type. Require inventory-wide caller/adapter/schema/manifest/fixture exhaustiveness with duplicate detection.
7. Complete recursive grammar semantics: nullable and missing behavior for every composite node; bounded opaque nodes; a real bounded node for unknown union children; timestamp-record key/value/timestamp/exclusion/equal-timestamp rules; construction-time rejection of incompatible policy/value combinations; deterministic ties and order; no silent truncation.
8. Validate raw request, stored candidate, and merged candidate paths through the manifest before any non-strict Zod normalization can strip keys. Define exact input 400 versus merge/stored/output 409 mapping and include a rogue-key route regression.
9. Require bijective set-equality proofs among actual useAuthSync callers, VALID_APP_IDS, PROGRESS_SCHEMAS, inventory entries, primary storage keys, declared legacy keys, adapters, manifests, and fixtures, with explicit exceptions only for genuinely unsynced IDs. Define primary-key precedence and explicit persisted-path exclusions such as achievements watermarks. Each fixture must produce both a semantically default state and an earned/meaningful state.
10. Add concrete per-app semantic policy expectations and expected merge-result oracles. Prove spendable balances, current streaks, and settings are LWW; only source-proven monotonic counters use max; best times use nullable min; and every collection has explicit semantics. Path coverage alone is insufficient.
11. Specify DELETE independently: authenticate/validate, begin transaction, set timeouts, acquire the same pair advisory lock, read after lock, then delete progress and matching leaderboard atomically. Missing-row 404 is decided only after the locked read. Include unit and real-DB locked-read, missing-row, timeout, rollback, and no-DML proofs.
12. Correct Cookie Clicker overflow to expect ProgressMergeConflictError before leaderboard extraction. Add a separate schema-valid controlled extractor seam or fixture for LeaderboardProjectionConflictError. Both cases require 409 and unchanged progress/leaderboard state.
13. Make the PostgreSQL suite executable under pnpm. Explicitly choose a dependency/pool ownership design. Preferred simple choice: add pg plus @types/pg to apps/web test dependencies, include apps/web/package.json and pnpm-lock.yaml in ownership, use a dedicated test-owned Pool for blocker/observer/seed work, and expose or provide an explicit close seam for the route package pool so database drop is reliable. If choosing another design, prove resolution and pool closure exactly.
14. Wire apps/web/package.json exactly so test:integration invokes bash scripts/run-progress-integration.sh. That script owns provisioning, canonical URL validation, schema push/preflight, dedicated Vitest config, cleanup, and traps on every failure path. Ordinary Vitest remains DB-free.
15. Make database role/setup exact: set and verify password_encryption=scram-sha-256 before role password creation, validate LOGIN/CREATEDB and the stored verifier/authentication path, create the dedicated database OWNER hanks_progress_test (or grant equivalent exact schema rights), validate every canonical URL component, and never use a second URL spelling.
16. Concurrent first save must hold the pair lock, start both POSTs, observe both as ungranted waiters, verify the row remains absent, then release. Retain the stronger two-waiter read-order and POST/DELETE cases.
17. Make 55P03 and 57014 independently reachable: lock_timeout lower than statement_timeout for 55P03; lock_timeout disabled/raised with statement_timeout lower for 57014. Assert 503, Retry-After, rollback, and unchanged rows for both.
18. Add the full six-case merged response matrix: merge omitted/false/true crossed with existing/absent row. Under safe create-or-merge semantics, existing-row responses return merged:true and absent-row responses false regardless of request flag.
19. Reuse the complete inventory fixture for every enabled leaderboard extractor and require validateProgress, extractLeaderboardScore, and leaderboardEntrySchema to agree. Do not rely only on reduced extractor samples.
20. Preserve and sharpen the dedicated initial-GET and re-fetch retry states, manual force/retry behavior, timeout behavior, completion gates, file inventory, task order, acceptance matrix, risk table, rollback/observability details, and focused/full verification commands.

Remove or rewrite every statement that conflicts with these rules. Search the complete document for stale wording after editing. Do not leave an open design choice where implementation would fork materially.

Before finishing, run deterministic plan checks: parse the HTML, run git diff --check, scan the plan for U+2014, and prove from git status/diff that product code/tests/config remain untouched and only the HTML plan changed during this pass beyond pre-existing docs/ and graphify-out/ artifacts. Report a concise summary and any unresolved blocker. Do not implement product code.
