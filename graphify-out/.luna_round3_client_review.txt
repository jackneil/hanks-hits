VERDICT: NEEDS ATTENTION

- **CRITICAL:** Owner epochs do not replace the persisted ownership barrier. Plan §“Global invalidation registry and epoch” (HTML 564-594) omits `PROGRESS_OWNER_KEY`, foreign-memory locking, hard reload, and the pagehide write-back barrier. `auth-client.ts:36-45` clears same-tab storage without resetting Zustand memory, while `useAuthSync.ts:223-266` proves the existing guard is required. Amend the plan to preserve this protocol and test A sign-out, write-back, B sign-in, and zero POST/beacon leakage.

- **CRITICAL:** Legacy hydration has no live-state migration/apply operation. Plan §“Exact hydration adapter contract” (HTML 382-478) declares Chess legacy key support but exposes only `adoptServer`, not `applyPersisted` or a migration rule; Chess persists only `hank-chess-state` (`store.ts:434-441`) while `chess-storage` remains legacy (`storage-keys.ts:20`). Define duplicate-key precedence, legacy-to-canonical application, and tests proving meaningful legacy data cannot be ignored or left out of live state.

- **MEDIUM:** The both-meaningful/server-present transition is nondeterministic. The truth table says “adopt the candidate or authoritative server state” and then reconcile (HTML 357), while the state machine sends the case directly to re-fetch (HTML 372-375). Specify the exact candidate choice, baseline, READY/reconcile ordering, acknowledgment, and re-fetch transition, with an ordered test.

- **MEDIUM:** Stable synthetic timestamps lack a logical-save allocation contract. `capture` requires a caller-supplied `logicalSaveId` (HTML 433-476), but no coordinator state or algorithm creates it from polling. Monster Truck and Oregon Trail return `Date.now()` on reads (`monster-truck/lib/store.ts:541-555`, `oregon-trail/lib/store.ts:173`). Add semantic-change detection, logical-ID lifecycle, timestamp reuse/reset rules, and repeated-poll tests.

- **MEDIUM:** Beacon readiness rules contradict each other. The plan forbids pre-READY beacons (HTML 350, 986), but §“Terminal beacon semantics” permits real unmount beacons and Hidden S0 beacons without a READY guard (HTML 598-603). Require every terminal event to suppress sends until exactly READY, including active bootstrap/re-fetch, and test each event in every pre-READY phase.

- **MEDIUM:** Response parsing and test observability are underspecified. “Expected JSON shape” and structured diagnostics are required (HTML 615, 1083-1087), but no typed GET/POST response envelopes or inspection/event seam is defined; the current hook exposes only status, timestamp, and `forceSync` (`useAuthSync.ts:26-31`). Define discriminated response/error types and an injectable or test-only coordinator state/diagnostic observer that proves markers, baselines, queues, epochs, and retry states.