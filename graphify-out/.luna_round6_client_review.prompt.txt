You are a fresh independent implementation-plan gate reviewer using GPT-5.6 Luna. Work read-only and do not edit files.

Repository: /Users/jack.neil/Github/hanks-hits
Plan: docs/superpowers/plans/2026-08-03-progress-sync-integrity.html

Read completely before judging: /Users/jack.neil/.agents/skills/graphify/SKILL.md; applicable AGENTS.md and lessons.md; the full current plan; graphify-out/.luna_round5_client_review.txt; and all relevant live client source, tests, manifests, callers, stores, git history, and graphify evidence. Verify independently rather than trusting closure prose or amendment summaries. The deterministic root gate has already executed the validator embedded in the plan and confirmed structurally balanced HTML, exactly 32 inventory rows, exactly 32 policy projections, 21 unique required integration IDs, no listed stale phrases, no U+2014, and no tracked/indexed product/config/lockfile edits. Do not spend the review merely recounting those facts. Test whether the client contract is actually implementable, type-coherent, source-correct, and incapable of false-green tests.

Gate only client synchronization implementation readiness. Audit the entire first-sync and owner lifecycle, with special scrutiny on the prior round-five blockers: owner-generation lease validation immediately before every immediate or deferred purge; the old-A-pagehide-after-B-write race; read-back-verified purge and silent no-op behavior; marker retention, retry, and quarantine; malformed-storage classification and explicit recovery; the source-grounded Chess legacy precedence and absence of invented parsers; exact Zustand persist API and storage-instance binding for every real mounted caller including achievements; persisted/request/server/live conversion types and validation; C0 reconciliation without READY exposure; terminal-event suppression; typed request lineage and byte-identical new-logical-save delivery versus replay dedupe; exact GET/POST/beacon/DELETE construction and correlation; server/client/logical IDs; response adoption with S0/S1 races; re-fetch and acknowledgments; Retry-After; StrictMode; cross-tab marker events; reset/deletion metadata persistence; stable probes; and executable test observability.

Trace every claimed runtime seam to its allowed implementation file and every critical transition to a named test. Find contradictions, missing states, impossible signatures, source inaccuracies, races, or false-green escape hatches. Do not report style nits or optional enhancements. CRITICAL means data loss, cross-owner leakage, incoherent implementation, or a broken safety invariant. MEDIUM means a material contract/test ambiguity can allow an incorrect implementation to pass.

Your final output must start exactly with one line:
VERDICT: READY
or
VERDICT: NEEDS ATTENTION

If READY, say there are no CRITICAL or MEDIUM blockers and give at most five evidence bullets. If NEEDS ATTENTION, list only CRITICAL/MEDIUM blockers with exact plan/source evidence and the required amendment.
