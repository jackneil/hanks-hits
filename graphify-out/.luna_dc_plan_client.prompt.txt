You are an independent GPT-5.6 Luna planning-phase double-check reviewer. You are READ-ONLY: do not edit files, run formatters, or commit.

## REVIEW SCOPE
Review the complete implementation and acceptance plan at:
docs/superpowers/plans/2026-08-03-progress-sync-integrity.html

The codebase is on branch fix/progress-sync-integrity at origin/master commit 7649017. The only planned tracked addition is that HTML file; product source is still unchanged. Read the plan completely, lessons.md, the exact current client implementation and relevant tests, plus all actual useAuthSync callers needed to validate assumptions:
- apps/web/src/shared/hooks/useAuthSync.ts
- apps/web/src/shared/hooks/__tests__/useAuthSync.test.tsx
- apps/web/src/shared/hooks/__tests__/useAuthSync.owner-guard.test.tsx
- apps/web/src/shared/hooks/__tests__/useAuthSync.achievements-observer.test.tsx
- relevant game/app call sites of forceSync
- apps/web/src/lib/storage-keys.ts and auth ownership helpers

## INTENT
The verified client defects are: autosave and force can POST before initial sync; a delayed merged re-fetch compares against a captured snapshot and can replace newer live state; failed saves mark data saved before success; multiple triggers can overlap; and a sibling hook can beacon during module-level foreign purge. The plan must preserve polling, debounce, initial hydration, ownership, achievements observation, beacons, and existing forceSync callers.

## REVIEW METHOD
Apply these lenses at planning depth:
1. Intent and requirements, including explicit must-never-do and impossible-state checks.
2. Guardrails and repository conventions.
3. Logic, React lifecycle, closures, fake-timer behavior, concurrency, logout/remount, StrictMode, unmount, stale callbacks, and errors.
4. Testing quality: prove each regression would fail before the fix and pass after it, without races or false confidence.
5. Maintainability and simplicity: find unnecessary state or a missing invariant that will make implementation brittle.

Ask naive questions that expose assumptions. In particular, trace every route by which READY can be reached or reset, every route by which queued force/save work can drain, and what happens when auth/logout/unmount/foreign purge occurs during an in-flight request. Check whether the plan defines behavior for failed initial GET, failed initial POST, non-OK re-fetch, and a queued save that changes again while a POST is settling.

Return a concise report with:
- VERDICT: READY or NEEDS ATTENTION
- CRITICAL findings, then MEDIUM findings, each with plan section and source file:line evidence
- LOW suggestions ranked by impact times effort
- Clean lenses collapsed to one line each
- Exact plan amendments required for every CRITICAL/MEDIUM

READY is allowed only with zero CRITICAL and zero MEDIUM findings. Do not edit the plan.
